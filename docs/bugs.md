OS WIDE
- Add Version Control System
- Add Software Updater
    - Easy way to push updates to PilkOS
    - Auto-generated changelogs
    - Retain ability to view changes made to the code live in the Electron app without affecting, but don't push each change as an update. Only if I click
    - Create a Dev side version of PilkOS that has developer only options.


DESKTOP
- Add Window Snapping feature.

DOCK
- Add live previews of windows when hovering over their app icon on the dock.
- Add ability to pin apps to the dock for quick access.
- Adjust height of dock to match Windows 11 taskbar height.
- Start Menu icon should be full based on currenty battery percentage*
- Remove App Labels completely, including the toggle in the Settings app.
- Make App icons on the Dock centered with the viewport instead of being centered accounting for things like the Start Menu icon and the Quick Settings icon.
- Add more conditions with their respective weather icons.
- Add a dot in the top right if there is a weather advisory or warning. Replace the tooltip text to show what the alert is for.
- Menus don't auto-collapse when another menu is open. For example, Quick Settings stays open when I open the Start Menu
-

START MENU
- Redesign the Start Menu.
- Add a Search box to search through applications.

QUICK SETTINGS
- Make button sizes consistent.
- Make diagonal lines in Quick Settings consistent.
- Remove battery percentage from the hidden battery tile text in Edit mode.
- Redesign tile sub-menus.
- Fix sub-menu height issues.
- Think of new tile ideas.
    - 
- Add tooltips to buttons.
- Shape of hidden tiles should match shape of tooltips
- When a 5th tile is added, the spacing at the bottom of the brightness slider to the bottom edge of the menu isn't right.
- In Edit Mode, X buttons should be in the same spot as the > button.

SCREENSHOTS
- When capturing screen, some elements are not captured properly such as the start menu icon and parts of the dock have a black area around them.

PERSONALIZATION
- Add Themes
    - Change accent color
